name: Retired Kuwagata Expedition Revision v2

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/build-kuwagata-revision-v2.yml'

concurrency:
  group: kuwagata-expedition-revision-v2
  cancel-in-progress: true

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Superseded by the exact-fast corrected render workflow.'
